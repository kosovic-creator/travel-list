{
  "configurations": [
    


    {"type":"node","request":"launch","name":"Run next","runtimeExecutable":"next","cwd":"${workspaceFolder}","args":[]},
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Next.js App",
      "program": "${workspaceFolder}/${input:programPath}",
      "runtimeExecutable": "npm",
      "runtimeArgs": [
        "run",
        "dev"
      ],
      "port": 3000,
      "webRoot": "${workspaceFolder}/src/app"
    }
  ],
  "inputs": [
    {
      "type": "promptString",
      "id": "programPath",
      "description": "Path to the entry point of your program (e.g., src/app/page.js)"
    }
  ]
}